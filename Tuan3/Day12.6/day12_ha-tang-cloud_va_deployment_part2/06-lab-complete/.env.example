# Copy thành .env.local và điền giá trị thật
# ❌ KHÔNG commit file .env.local lên Git

# Server
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=development
DEBUG=true

# App
APP_NAME=MediLich
APP_VERSION=1.0.0

# LLM (bỏ trống để dùng mock)
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini

# VietOCR sidecar (bỏ trống/localhost để dùng mock)
VIETOCR_URL=http://localhost:5001

# Security — BẮT BUỘC thay trong production
AGENT_API_KEY=dev-key-change-me-in-production
JWT_SECRET=dev-jwt-secret-change-in-production

# Rate & Budget
RATE_LIMIT_PER_MINUTE=10
DAILY_BUDGET_USD=5.0

# Storage
REDIS_URL=redis://localhost:6379/0

# CORS
ALLOWED_ORIGINS=http://localhost:3000
